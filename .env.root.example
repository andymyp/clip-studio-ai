# Docker Compose host ports and infrastructure credentials.
# Copy this file to .env.root and pass it with:
# docker compose --env-file .env.root up --build

# APP
FRONTEND_PORT=3000
BACKEND_PORT=3001
WORKER_API_PORT=3002

# DATABASE
POSTGRES_USER=clipstudio
POSTGRES_PASSWORD=clipstudio
POSTGRES_DB=clipstudio
POSTGRES_PORT=5432

# REDIS
REDIS_PORT=6379

# OLLAMA
OLLAMA_PORT=11434

# YT-DLP
# Netscape-format YouTube cookie export mounted read-only into worker-jobs.
YTDLP_COOKIE_FILE_HOST=./storage/secrets/youtube-cookies.txt
